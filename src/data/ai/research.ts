/* ─────────────────────────────────────────────
   The research section's one link target.

   Split out of the old `src/data/ai.ts` when that file became the directory
   holding the Oxy AI taxonomy, claims and editorial copy. The section itself is
   shared by `/` and `/ai`, so its constants outlive the page that introduced
   them.
   ───────────────────────────────────────────── */

export const researchCtaHref = '/initiative'

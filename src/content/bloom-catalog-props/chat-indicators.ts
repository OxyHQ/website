// Generated by scripts/generate-bloom-catalog.ts. Do not edit.

import type { BloomSurfaceProps } from '../bloom-catalog'

export const props: BloomSurfaceProps = {
  subpath: 'chat-indicators',
  propTypes: {
    'PresenceDotProps': {
      props: [
        { name: 'status', type: 'PresenceStatus', options: ['online', 'idle', 'offline', 'busy'], optional: false },
        { name: 'size', type: 'PresenceDotSize', options: ['small', 'medium', 'large'], optional: true, description: 'Default `medium`.' },
        { name: 'ringColor', type: 'string', optional: true, description: "The colour of the ring that separates the dot from whatever is behind it — the page or card surface it sits on. Default `theme.colors.background`; pass the card's own colour when the row is on a raised surface." },
        { name: 'ringWidth', type: 'number', optional: true, description: 'Ring thickness. Default `2`. `0` draws no ring.' },
        { name: 'accessibilityLabel', type: 'string', optional: true, description: 'The accessible name. Defaults to the English word for the status ("Online", "Away", "Offline", "Busy") — pass a translated one. An empty string hides the dot from assistive tech, for rows that already say the status in their own text.' },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true },
        { name: 'testID', type: 'string', optional: true },
      ],
    },
    'AvatarPresenceProps': {
      props: [
        { name: 'status', type: 'PresenceStatus', options: ['online', 'idle', 'offline', 'busy'], optional: true, description: 'The status drawn in the corner. Omit to draw the avatar alone.' },
        { name: 'presenceSize', type: 'PresenceDotSize', options: ['small', 'medium', 'large'], optional: true, description: "Overrides the dot size picked from the avatar's own size." },
        { name: 'presenceRingColor', type: 'string', optional: true, description: "Forwarded to the dot's `ringColor`." },
        { name: 'presenceLabel', type: 'string', optional: true, description: "Forwarded to the dot's `accessibilityLabel`." },
        { name: 'containerStyle', type: 'StyleProp<ViewStyle>', optional: true, description: 'Style for the wrapper that positions the dot (not the avatar itself).' },
        { name: 'source', type: 'string | ImageSourcePropType | null', optional: true, description: "Flexible image source — accepts a URL string, an ImageSourcePropType (e.g. require('./img.png') or { uri: '...' }), or null/undefined. Takes precedence over the `uri` prop when both are provided." },
        { name: 'uri', type: 'string', optional: true, description: 'Direct URI string. Use `source` for more flexible input.' },
        { name: 'variant', type: 'string', optional: true, description: "Rendition variant forwarded to the ImageResolver when `source` is a bare file ID (a non-URL string). Selects a server-side rendition such as `'thumb'`, `'small'`, or `'medium'`. Defaults to `'thumb'` so an avatar never accidentally requests the full-size original; pass an explicit variant (e.g. a larger rendition) when you need a bigger image. Ignored when `source` is already a full URL/`{uri}` or when no resolver is registered." },
        { name: 'fallbackSource', type: 'ImageSourcePropType', optional: true, description: 'Fallback image source when source/uri is missing or errors (defaults to colored circle)' },
        { name: 'size', type: 'number | AvatarSizeToken', optional: true, description: "Avatar size: a diameter in pixels, or one of the size rungs (`'xs'` 20, `'sm'` 24, `'md'` 32, `'lg'` 36). Defaults to 40." },
        { name: 'color', type: 'AvatarColor', options: ['neutral', 'blue', 'lime', 'pink'], optional: true, description: "Tint of the initials disc. Defaults to a tint derived deterministically from `name`, or `'neutral'` for explicit `initials`." },
        { name: 'initials', type: 'string', optional: true, description: 'Explicit initials for the fallback disc, e.g. `"M"` or `"AL"`. Wins over the letter derived from `name`.' },
        { name: 'alt', type: 'string', optional: true, description: 'Accessible description of the photo. When omitted the avatar is decorative unless `onPress` makes it a control named by `name`.' },
        { name: 'verified', type: 'boolean', optional: true, description: 'Whether to show a verified badge' },
        { name: 'verifiedIcon', type: 'ReactNode', optional: true, description: 'Custom verified badge icon (rendered at bottom-right)' },
        { name: 'shape', type: 'AvatarShape | AvatarShapePath', optional: true, description: "Shape of the avatar (defaults to `'circle'`). Accepts a built-in name — `'squircle'`, `'heart'`, `'pentagon'`, … see AvatarShapeName — or an AvatarShapePath with your own outline. Everything except `'circle'` clips through `react-native-svg`; a name that does not resolve falls back to a circle." },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true, description: 'Container style' },
        { name: 'imageStyle', type: 'StyleProp<ImageStyle>', optional: true, description: 'Image style (circle shape only)' },
        { name: 'placeholderColor', type: 'string', optional: true, description: 'Custom background color for the placeholder circle (overrides the tint)' },
        { name: 'placeholderIcon', type: 'ReactNode', optional: true, description: 'Custom icon rendered inside the placeholder circle when no image is available' },
        { name: 'name', type: 'string', optional: true, description: 'Contact/user name used to derive a deterministic initial and tint. When no image resolves (missing `source`/`uri` or image error), the Avatar renders a tinted initials disc with the first letter of the name. Consumers can still override via `placeholderColor` / `placeholderIcon`.' },
        { name: 'onPress', type: '() => void', optional: true, description: 'Press handler — wraps the avatar in a `Pressable` when provided.' },
        { name: 'live', type: 'boolean', optional: true, description: 'When true, marks the user as live-streaming: draws a solid red ring that overlays the avatar edge and, unless `hideLiveBadge` is set, a small red "LIVE" pill hanging off the bottom-center. Static (no animation). The badge is only shown for `size > 16`.' },
        { name: 'hideLiveBadge', type: 'boolean', optional: true, description: 'Suppress the "LIVE" pill badge while keeping the live ring (ring only).' },
        { name: 'liveLabel', type: 'string', optional: true, description: "Text shown inside the live badge. Defaults to `'LIVE'`. Provide a localized string for i18n — keep it extremely short (≈4 characters) as there is very little room." },
        { name: 'liveColor', type: 'string', optional: true, description: 'Override color for the live ring and badge background. Defaults to the theme `negative` token. Prefer the theme default; only override for brand-specific live treatments.' },
        { name: 'ring', type: 'AvatarRingConfig', optional: true, description: 'Decorative ring drawn around the avatar. Pass a single color string for a solid ring, or an array of 2+ colors for a gradient ring (e.g. an Instagram-stories ring). The gradient form requires `react-native-svg` (falls back to a solid ring of the first color if it is not installed).' },
        { name: 'testID', type: 'string', optional: true },
      ],
    },
    'MessageStatusProps': {
      props: [
        { name: 'status', type: 'MessageDeliveryStatus', options: ['sending', 'sent', 'delivered', 'read', 'failed'], optional: false },
        { name: 'size', type: 'MessageStatusSize', optional: true, description: 'Default `14`.' },
        { name: 'color', type: 'string', optional: true, description: 'Overrides the glyph colour — for ticks drawn on a coloured bubble, where the muted text colour would not survive. `failed` ignores it and stays negative: an error that is repainted to match the bubble stops reading as an error.' },
        { name: 'label', type: 'string', optional: true, description: 'The accessible name. Defaults per status: "Sending…", "Sent", "Delivered", "Read", "Not sent". An empty string hides the glyph from assistive tech.' },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true },
        { name: 'testID', type: 'string', optional: true },
      ],
    },
    'UnreadBadgeProps': {
      props: [
        { name: 'count', type: 'number', optional: true, description: 'Unread messages. `0` or less renders NOTHING unless `dot` is set.' },
        { name: 'max', type: 'number', optional: true, description: 'Above this the pill reads `"99+"`. Default `99`.' },
        { name: 'dot', type: 'boolean', optional: true, description: 'Draw a plain dot with no number (the count still names it).' },
        { name: 'muted', type: 'boolean', optional: true, description: 'Neutral fill instead of the accent — a muted conversation.' },
        { name: 'size', type: 'UnreadBadgeSize', options: ['small', 'medium'], optional: true, description: 'Default `medium`.' },
        { name: 'formatLabel', type: '(count: number) => string', optional: true, description: 'Builds the accessible name from the count. Default English: "1 unread message" / "3 unread messages", and "Unread" for a `dot` with no count. It receives the REAL count, not the clamped text.' },
        { name: 'accessibilityLabel', type: 'string', optional: true, description: 'Replaces the composed name entirely.' },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true },
        { name: 'textStyle', type: 'StyleProp<TextStyle>', optional: true },
        { name: 'testID', type: 'string', optional: true },
      ],
    },
    'TypingDotsProps': {
      props: [
        { name: 'size', type: 'number', optional: true, description: 'Dot diameter in px. Default `6`.' },
        { name: 'color', type: 'string', optional: true, description: 'Dot colour. Default: the secondary text colour.' },
        { name: 'label', type: 'string', optional: true, description: 'Drawn beside the dots and used as the accessible name ("Ana is typing…"). Without it the component is decorative and hidden from assistive tech — a bare bouncing dot has nothing to announce.' },
        { name: 'labelStyle', type: 'StyleProp<TextStyle>', optional: true, description: 'Style for the label text.' },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true },
        { name: 'testID', type: 'string', optional: true },
      ],
    },
    'StoryRingProps': {
      props: [
        { name: 'state', type: 'StoryRingState', options: ['unseen', 'seen', 'none'], optional: true, description: 'Default `unseen`.' },
        { name: 'size', type: 'number', optional: false, description: 'The diameter of the avatar it wraps. The footprint is bigger — see the doc.' },
        { name: 'thickness', type: 'number', optional: true, description: 'Ring stroke width. Default `2`.' },
        { name: 'gap', type: 'number', optional: true, description: 'Space between the avatar edge and the ring. Default `2`.' },
        { name: 'colors', type: 'string | string[]', optional: true, description: 'Ring colours. One string for a solid ring, 2+ for a gradient. Defaults to an accent gradient for `unseen` and the hairline border for `seen`.' },
        { name: 'children', type: 'ReactNode', optional: true, description: 'The avatar (or anything else) the ring wraps.' },
        { name: 'badge', type: 'ReactNode', optional: true, description: 'An affordance pinned to the bottom-right — a `+ N` pill or an "Add" button.' },
        { name: 'onPress', type: '() => void', optional: true, description: 'Wraps the ring in a pressable named by `accessibilityLabel`.' },
        { name: 'accessibilityLabel', type: 'string', optional: true, description: 'Required when `onPress` is set: what opening the ring does.' },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true },
        { name: 'testID', type: 'string', optional: true },
      ],
    },
  },
  components: [
    { name: 'PresenceDot', propsType: 'PresenceDotProps' },
    { name: 'AvatarPresence', propsType: 'AvatarPresenceProps' },
    { name: 'MessageStatus', propsType: 'MessageStatusProps' },
    { name: 'UnreadBadge', propsType: 'UnreadBadgeProps' },
    { name: 'TypingDots', propsType: 'TypingDotsProps' },
    { name: 'StoryRing', propsType: 'StoryRingProps' },
  ],
}

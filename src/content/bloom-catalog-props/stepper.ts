// Generated by scripts/generate-bloom-catalog.ts. Do not edit.

import type { BloomSurfaceProps } from '../bloom-catalog'

export const props: BloomSurfaceProps = {
  subpath: 'stepper',
  propTypes: {
    'StepperProps': {
      props: [
        { name: 'value', type: 'number', optional: false, description: 'The current value. The stepper is fully controlled.' },
        { name: 'onValueChange', type: '(value: number) => void', optional: false, description: 'Called with the next value, already clamped to `min`..`max` and snapped to `step`.' },
        { name: 'min', type: 'number', optional: true, description: 'Lowest value; the decrement button disables here. Default `0`.' },
        { name: 'max', type: 'number', optional: true, description: 'Highest value; the increment button disables here. Default: unbounded.' },
        { name: 'step', type: 'number', optional: true, description: 'Amount one press adds or removes. Default `1`.' },
        { name: 'disabled', type: 'boolean', optional: true, description: 'Disables both buttons and the keyboard.' },
        { name: 'size', type: 'StepperSize', options: ['small', 'medium'], optional: true, description: 'Default `medium`.' },
        { name: 'formatValue', type: '(value: number) => string', optional: true, description: 'How the value is drawn and announced (`aria-valuetext`), e.g. `(n) => `${n}+``.' },
        { name: 'accessibilityLabel', type: 'string', optional: true, description: "The control's accessible name." },
        { name: 'decrementLabel', type: 'string', optional: true, description: 'Name of the `−` button. Default `"Decrease"`.' },
        { name: 'incrementLabel', type: 'string', optional: true, description: 'Name of the `+` button. Default `"Increase"`.' },
        { name: 'onRemove', type: '() => void', optional: true, description: 'Opt-in remove at the floor: at `min` the `−` button becomes a trash button that calls this instead of disabling (a basket line going from 1 to gone). Only the button removes — the keyboard and accessibility actions on the value still stop at `min`. Without it the stepper floors at `min`.' },
        { name: 'removeLabel', type: 'string', optional: true, description: 'Name of the trash button drawn at `min` with `onRemove`. Default `"Remove"`.' },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true },
        { name: 'testID', type: 'string', optional: true },
      ],
    },
    'StepperRowProps': {
      props: [
        { name: 'title', type: 'ReactNode', optional: false, description: 'The row\'s title ("Adults"); also the stepper\'s accessible name unless one is given.' },
        { name: 'description', type: 'ReactNode', optional: true, description: 'A secondary line under the title ("Ages 13 or above").' },
        { name: 'divider', type: 'boolean', optional: true, description: 'Draws a hairline under the row, for stacked rows. Default `false`.' },
        { name: 'accessibilityLabel', type: 'string', optional: true, description: "Overrides the stepper's name; required when `title` is not a string." },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true, description: 'Style of the row container.' },
        { name: 'stepperStyle', type: 'StyleProp<ViewStyle>', optional: true, description: 'Style of the stepper inside the row.' },
        { name: 'size', type: 'StepperSize', options: ['small', 'medium'], optional: true, description: 'Default `medium`.' },
        { name: 'disabled', type: 'boolean', optional: true, description: 'Disables both buttons and the keyboard.' },
        { name: 'testID', type: 'string', optional: true },
        { name: 'value', type: 'number', optional: false, description: 'The current value. The stepper is fully controlled.' },
        { name: 'onValueChange', type: '(value: number) => void', optional: false, description: 'Called with the next value, already clamped to `min`..`max` and snapped to `step`.' },
        { name: 'removeLabel', type: 'string', optional: true, description: 'Name of the trash button drawn at `min` with `onRemove`. Default `"Remove"`.' },
        { name: 'step', type: 'number', optional: true, description: 'Amount one press adds or removes. Default `1`.' },
        { name: 'onRemove', type: '() => void', optional: true, description: 'Opt-in remove at the floor: at `min` the `−` button becomes a trash button that calls this instead of disabling (a basket line going from 1 to gone). Only the button removes — the keyboard and accessibility actions on the value still stop at `min`. Without it the stepper floors at `min`.' },
        { name: 'formatValue', type: '(value: number) => string', optional: true, description: 'How the value is drawn and announced (`aria-valuetext`), e.g. `(n) => `${n}+``.' },
        { name: 'min', type: 'number', optional: true, description: 'Lowest value; the decrement button disables here. Default `0`.' },
        { name: 'max', type: 'number', optional: true, description: 'Highest value; the increment button disables here. Default: unbounded.' },
        { name: 'decrementLabel', type: 'string', optional: true, description: 'Name of the `−` button. Default `"Decrease"`.' },
        { name: 'incrementLabel', type: 'string', optional: true, description: 'Name of the `+` button. Default `"Increase"`.' },
      ],
    },
  },
  components: [
    { name: 'Stepper', propsType: 'StepperProps' },
    { name: 'StepperRow', propsType: 'StepperRowProps' },
  ],
}

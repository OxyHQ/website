// Generated by scripts/generate-bloom-catalog.ts. Do not edit.

import type { BloomSurfaceProps } from '../bloom-catalog'

export const props: BloomSurfaceProps = {
  subpath: 'note-card',
  propTypes: {
    'NoteCardProps': {
      props: [
        { name: 'title', type: 'string', optional: false, description: "The note's title. One line in `row`, two in `grid`; truncated, never wrapped away." },
        { name: 'excerpt', type: 'string', optional: true, description: 'The opening of the body, as plain text. Clamped — four lines in `grid`, one in `row`. Ignored when `checklist` is given: a checklist note previews as a checklist, because a flattened one reads as a paragraph of fragments.' },
        { name: 'checklist', type: 'ReadonlyArray<NoteChecklistItem>', optional: true, description: "The note's body when it IS a checklist. The first few items with their ticked state, drawn in `Checkbox`'s own box — read-only, so nothing here takes a press." },
        { name: 'checklistTotal', type: 'number', optional: true, description: 'How many items the checklist has in total, when `checklist` is only its head. Drives the "+N more" line. Defaults to `checklist.length`.' },
        { name: 'tags', type: 'ReadonlyArray<string>', optional: true, description: 'The note\'s tags, as drawn. Rendered as `Chip`s; the overflow becomes "+N".' },
        { name: 'maxTags', type: 'number', optional: true, description: 'How many tags to draw before the "+N" pill. Default `3` in `grid`, `2` in `row`.' },
        { name: 'meta', type: 'NoteCardMeta', optional: true, description: 'The metadata trail. Every member is optional and an absent one draws nothing.' },
        { name: 'pinned', type: 'boolean', optional: true, description: 'Draws the pin marker in the corner (`grid`) or before the title (`row`).' },
        { name: 'tone', type: 'AccentTone', options: ['default', 'primary', 'secondary', 'tertiary', 'success', 'warning', 'error', 'info'], optional: true, description: "The note's colour, from Bloom's accent tones — the card is washed in that tone's subtle tint and every quiet line is re-measured against the WASH, not against the page. There is no hex here on purpose: a note colour picked by eye is the one that disappears in dark mode." },
        { name: 'density', type: 'NoteCardDensity', options: ['grid', 'row'], optional: true, description: 'Default `grid`.' },
        { name: 'selected', type: 'boolean', optional: true, description: "Marks the card as the current one (a board's focused note, a split view's open note)." },
        { name: 'selectable', type: 'boolean', optional: true, description: 'Turns on multi-select: a checkbox takes the leading slot and a press on the card TOGGLES rather than opens. This is the state a notes app spends its bulk actions in, so it is a mode of the card rather than a second component.' },
        { name: 'onSelectedChange', type: '(selected: boolean) => void', optional: true, description: 'Called with the next selected state while `selectable`.' },
        { name: 'onPress', type: '() => void', optional: true, description: 'Called when the card is opened. Not called while `selectable`.' },
        { name: 'onLongPress', type: '() => void', optional: true, description: "A long press — the app's shortcut into selection mode or a sheet." },
        { name: 'loading', type: 'boolean', optional: true, description: 'Draws the skeleton in the same geometry. See `NoteCardSkeleton`.' },
        { name: 'accessibilityLabel', type: 'string', optional: true, description: 'Replaces the composed accessible name ("Harbour notes, Pinned, 3 items, 2 min ago"), which is English.' },
        { name: 'labels', type: 'NoteCardLabels', optional: true, description: 'Replaces the English words above.' },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true },
        { name: 'testID', type: 'string', optional: true },
      ],
    },
    'NoteCardSkeletonProps': {
      props: [
        { name: 'density', type: 'NoteCardDensity', options: ['grid', 'row'], optional: true, description: 'Match the card it stands in for. Default `grid`.' },
        { name: 'lines', type: 'number', optional: true, description: 'How many excerpt lines to draw. Default `3` in `grid`, `1` in `row`.' },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true },
        { name: 'testID', type: 'string', optional: true },
      ],
    },
  },
  components: [
    { name: 'NoteCard', propsType: 'NoteCardProps' },
    { name: 'NoteCardSkeleton', propsType: 'NoteCardSkeletonProps' },
  ],
}

// Generated by scripts/generate-bloom-catalog.ts. Do not edit.

import type { BloomSurfaceProps } from '../bloom-catalog'

export const props: BloomSurfaceProps = {
  subpath: 'user-hover-card',
  propTypes: {
    'UserHoverCardProps': {
      props: [
        { name: 'avatar', type: 'string | null', optional: true, description: "Avatar image source. Accepts a full image URL OR a resolver-handled id (e.g. an Oxy file ID) — it is routed into Avatar 's `source` prop, so non-URL strings resolve through the consumer's ImageResolver. Null/undefined falls back to the deterministic name-based placeholder." },
        { name: 'variant', type: 'string', optional: true, description: "Rendition variant forwarded to Avatar 's `variant` prop when `avatar` is a bare file ID. Omit for the full-size rendition (the default for this larger identity avatar)." },
        { name: 'displayName', type: 'string', optional: false, description: 'Canonical, already-resolved display name (an API contract — render directly).' },
        { name: 'username', type: 'string', optional: true, description: 'Handle without the leading `@` (the component prefixes it).' },
        { name: 'bio', type: 'string', optional: true, description: 'Optional short biography; truncated to a few lines.' },
        { name: 'stats', type: 'UserHoverCardStat[]', optional: true, description: 'Optional metrics row (e.g. Following / Followers).' },
        { name: 'verified', type: 'boolean', optional: true, description: 'Whether to show the verified badge next to the display name.' },
        { name: 'cover', type: 'string | null', optional: true, description: "Optional cover image drawn full-bleed across the top of the card, with the avatar overlapping its bottom edge. Accepts a full URL OR a resolver-handled id, exactly like avatar: a non-URL string goes through the consumer's ImageResolver (with coverVariant). Omit for the compact card without a cover." },
        { name: 'coverVariant', type: 'string', optional: true, description: 'Rendition variant forwarded to the ImageResolver for a bare-id cover.' },
        { name: 'loading', type: 'boolean', optional: true, description: "Renders the card's skeleton — avatar disc, name and handle bars, bio lines and stat tiles in the same geometry — while the profile is being fetched. The card announces itself busy (`aria-busy` on web, `accessibilityState.busy` on native). `action` and `footer` are not rendered while loading." },
        { name: 'animateIn', type: 'boolean', optional: true, description: 'Plays the floating-panel entrance on mount (fade + `scale-95` + `blur-[2px]` on web, 150ms `ease-out`) — the motion menus and popovers use. Default `true`; skipped under reduced motion.' },
        { name: 'onPressProfile', type: '() => void', optional: true, description: 'Makes the identity area (avatar + name + handle) pressable.' },
        { name: 'action', type: 'ReactNode', optional: true, description: "Slot for a consumer-injected action — typically the app's SDK FollowButton. Bloom does NOT implement follow logic; it only renders the card." },
        { name: 'badge', type: 'ReactNode', optional: true, description: 'Marker slot in the handle row, rendered after `@username` — an account-kind or federation marker, the kind of quiet glyph that says what this account IS. Renders even when `username` is absent, because it describes the account rather than the handle. A long handle truncates to make room; the marker never shrinks.' },
        { name: 'footer', type: 'ReactNode', optional: true, description: 'Trailing content region, rendered after the stats row and spanning the full inner width — the slot for whatever this app knows about a person that Bloom does not (a contribution graph, a shared-followers strip, a mutual-friends line). Bloom adds the vertical rhythm and nothing else: no label, no divider, no clip.' },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true, description: 'Container style override.' },
        { name: 'testID', type: 'string', optional: true },
      ],
    },
  },
  components: [
    { name: 'UserHoverCard', propsType: 'UserHoverCardProps' },
  ],
}

// Generated by scripts/generate-bloom-catalog.ts. Do not edit.

import type { BloomSurfaceProps } from '../bloom-catalog'

export const props: BloomSurfaceProps = {
  subpath: 'file-upload',
  propTypes: {
    'FileUploadProps': {
      props: [
        { name: 'onFileSelected', type: '(file: FileUploadFile) => void', optional: true, description: 'Called with a file that passed validation, the moment it is accepted — start the real upload here and feed `progress` back.' },
        { name: 'onUploadComplete', type: '(file: FileUploadFile) => void', optional: true, description: 'Called after the progress and success states finish.' },
        { name: 'onReject', type: '(message: string, file: FileUploadFile) => void', optional: true, description: 'Called with the message shown when a file is rejected.' },
        { name: 'progress', type: 'number', optional: true, description: 'Upload progress, 0–100. When set, the component is CONTROLLED: it shows this value instead of its own simulated progress, and enters the success state once it reaches 100. Omit it for the self-running demo behaviour.' },
        { name: 'file', type: 'FileUploadFile | null', optional: true, description: 'The file being uploaded. When set (including `null`), the component is FULLY controlled: `null` is the idle drop zone, a file with `progress` below 100 is uploading, and at 100 it shows the success state, calls `onUploadComplete` after the hold, and waits for you to clear `file`. Set it from `onFileSelected`.' },
        { name: 'onPickFiles', type: '() => Promise<FileUploadFile | readonly FileUploadFile[] | null | undefined> | FileUploadFile | readonly FileUploadFile[] | null | undefined', optional: true, description: 'Opens a picker and resolves the chosen file(s); the first is used. REQUIRED on native, which has no built-in picker (wire `expo-document-picker` or similar). On web it replaces the built-in `<input type="file">`.' },
        { name: 'allowedExtensions', type: 'readonly string[]', optional: true, description: 'Accepted filename extensions, without dots. Default pdf, jpg, jpeg, png, xlsx.' },
        { name: 'maxBytes', type: 'number', optional: true, description: 'Maximum accepted size in bytes. Default 8 MB.' },
        { name: 'renderFileIcon', type: '(file: FileUploadFile) => ReactNode', optional: true, description: 'Overrides the icon shown while a file uploads (24px).' },
        { name: 'labels', type: 'Partial<FileUploadLabels>', optional: true },
        { name: 'disabled', type: 'boolean', optional: true },
        { name: 'accessibilityLabel', type: 'string', optional: true, description: 'Accessible name of the drop zone. Default "Upload a file".' },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true },
        { name: 'testID', type: 'string', optional: true },
      ],
    },
  },
  components: [
    { name: 'FileUpload', propsType: 'FileUploadProps' },
  ],
}

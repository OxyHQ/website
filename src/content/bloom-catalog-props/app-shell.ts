// Generated by scripts/generate-bloom-catalog.ts. Do not edit.

import type { BloomSurfaceProps } from '../bloom-catalog'

export const props: BloomSurfaceProps = {
  subpath: 'app-shell',
  propTypes: {
    'AppShellProps': {
      props: [
        { name: 'list', type: 'ReactNode', optional: true, description: '`split`: the first pane. `children` is the detail pane beside it.' },
        { name: 'children', type: 'ReactNode', optional: true },
        { name: 'variant', type: 'AppShellVariant', options: ['dashboard', 'feed', 'split', 'focus', 'canvas'], optional: true, description: 'The layout shape. Defaults to `dashboard` — the behaviour this component had before variants existed, down to the pixel.' },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true },
        { name: 'testID', type: 'string', optional: true },
        { name: 'title', type: 'string', optional: true, description: 'Heading over the content. Optional: without it (and without `breadcrumb` / `actions`) the header still renders the menu button while the sidebar is a drawer, and nothing otherwise. A page with its own `header` opens the drawer with `AppShellMenuButton` or `useAppShell()`.' },
        { name: 'info', type: 'ReactNode', optional: true, description: '`split`: an optional third pane, from `infoFrom`.' },
        { name: 'sidebar', type: "Omit<SidebarProps, 'mobile' | 'onClose'>", optional: true, description: "The rail. Rendered in flow at `lg` and up (`sm` and up with `variant: 'rail'`); below it, the same props drive the drawer (`mobile`, and `flat` for `reveal`), always as the panel. Sidebar configuration, including card (detached, default) or docked surface. Also applies with navigation." },
        { name: 'drawer', type: 'AppShellDrawer', options: ['overlay', 'reveal'], optional: true, description: 'How the rail arrives below `navFrom`. `reveal` slides the WHOLE page sideways for dashboard and compact feed layouts. Feed desktop columns retain their normal layout. Other variants use the overlay drawer.' },
        { name: 'breadcrumb', type: 'ReactNode', optional: true, description: 'Breadcrumb trail above the heading (a `Breadcrumb`).' },
        { name: 'actions', type: 'ReactNode', optional: true, description: 'Header actions on the right (`NotificationBell`, buttons).' },
        { name: 'header', type: 'ReactNode', optional: true, description: 'Replaces the whole header. Put an `AppShellMenuButton` in it to keep the drawer reachable on narrow screens.' },
        { name: 'aside', type: 'ReactNode', optional: true, description: 'A second column on the right of the page (a detail panel, activity, a chat). In flow from `asideFrom`; below it, stacked under the content or hidden (`asideCollapse`). Pinned like the sidebar — with document scroll it stays in document flow and sticks after tall content reaches its lower edge. Container/fixed modes retain an independently scrolling aside.' },
        { name: 'asideWidth', type: 'number', optional: true, description: "The aside column's width. Defaults to 320." },
        { name: 'asideFrom', type: 'AppShellBreakpoint', optional: true, description: 'The breakpoint the aside sits beside the content from. Defaults to `xl` (1280).' },
        { name: 'asideCollapse', type: "'stack' | 'hidden'", options: ['stack', 'hidden'], optional: true, description: 'Below `asideFrom`: `stack` (default) renders it after the content, `hidden` drops it. `canvas` always drops it — the screen is the canvas, so there is no column underneath to stack into.' },
        { name: 'contentMaxWidth', type: 'number', optional: true, description: "The content column's max width. Defaults to 1300." },
        { name: 'overlay', type: 'ReactNode', optional: true, description: 'Floating extras rendered last, e.g. a `ProOfferCard`.' },
        { name: 'drawerOpen', type: 'boolean', optional: true, description: 'Controlled drawer state (below `lg`).' },
        { name: 'onDrawerOpenChange', type: '(open: boolean) => void', optional: true },
        { name: 'contentWidth', type: 'number', optional: true, description: "`feed` / `focus`: the reading column's width. Default 600 — the measure a single column of body text stays readable at. It is a MAXIMUM on a narrow screen: the column shrinks to the space it has rather than overflowing." },
        { name: 'navigationAlign', type: "'edge' | 'content'", options: ['edge', 'content'], optional: true, description: 'edge keeps navigation at the shell edge; content centers it with the reading column and aside.' },
        { name: 'navigationGap', type: 'number', optional: true, description: 'Gap between the in-flow navigation and content. Defaults to the column gap.' },
        { name: 'asideGap', type: 'number', optional: true, description: 'feed/focus: gap between the reading column and aside. Defaults to the column gap.' },
        { name: 'gutter', type: 'number', optional: true, description: "The space between the shell's edge and its regions. Default 12 for `dashboard` (which also keeps its historical 16 column gap, so existing pages do not move) and 16 for the other variants, where one number drives both." },
        { name: 'navFrom', type: 'AppShellBreakpoint', optional: true, description: "The width the nav sits in FLOW from; below it the same nav is a drawer. Defaults to the tier the sidebar's own variant implies — `sm` for a rail (80px fits early), `lg` for the full panel — and to `sm` whenever `navExpandedFrom` is set, because below that tier the nav IS a rail." },
        { name: 'navExpandedFrom', type: 'AppShellBreakpoint', optional: true, description: 'Icons below, labels from here up: the shell swaps `sidebar.variant` between `rail` and `panel` at this width. Unset (the default) the sidebar keeps whatever variant it was given at every width.' },
        { name: 'panel', type: 'boolean', optional: true, description: "`feed`: wrap the centre column in a `ContentPanel`, so the page background reads as a gutter around a framed surface. The shell renders the panel — a page that renders its own inside this one trips `ContentPanel`'s nesting guard. Screens own their content padding; the shell adds no horizontal inset." },
        { name: 'panelColorPreset', type: 'AppColorName', options: ['teal', 'blue', 'green', 'yellow', 'red', 'purple', 'pink', 'sky', 'orange', 'mint', 'oxy', 'faircoin', 'pumpkin', 'gray', 'brown', 'peach', 'rose', 'mono', 'grove', 'ember', 'merlot', 'cobalt', 'jade', 'pine', 'olive', 'lagoon', 'navy', 'azure', 'violet', 'lavender', 'plum', 'cherry', 'chocolate', 'graphite', 'forest-fire', 'malachite-rush', 'moss-voltage', 'viridian-orbit', 'acid-canopy', 'pacific-flare', 'reef-pulse', 'electric-tide', 'arctic-signal', 'ultramarine-gold', 'midnight-citrus', 'persimmon-wave', 'solar-flux', 'paprika-current', 'crimson-spark', 'flamenco-sun', 'saffron-depth', 'clay-current', 'bronze-neon', 'copper-field', 'umber-signal', 'amethyst-current', 'ruby-current', 'orchid-flare', 'garnet-acid', 'indigo-ember', 'raspberry-gold', 'ink-orange', 'charcoal-lime', 'pewter-current'], optional: true, description: 'Theme only the central reading column; navigation, aside and framing retain the app theme.' },
        { name: 'framedFrom', type: 'ContentPanelFramedBreakpoint', optional: true, description: 'Optional independent framing breakpoint. By default a shell with navigation frames its panel exactly while the sidebar/rail is in flow.' },
        { name: 'pane', type: 'AppShellPane', options: ['list', 'detail', 'info'], optional: true, description: '`split`: which single pane renders below `splitFrom`. Default `list`.' },
        { name: 'listWidth', type: 'number', optional: true, description: "`split`: the list pane's resting width (360); a drag moves it in range." },
        { name: 'listMinWidth', type: 'number', optional: true, description: '`split`: the range a drag may reach. Defaults 280 and 520.' },
        { name: 'listMaxWidth', type: 'number', optional: true },
        { name: 'onListWidthChange', type: '(width: number) => void', optional: true, description: '`split`: reports the width a drag settled on.' },
        { name: 'infoWidth', type: 'number', optional: true, description: "`split`: the info pane's width. Default 320." },
        { name: 'splitFrom', type: 'AppShellBreakpoint', optional: true, description: '`split`: the width two panes fit from. Default `md` (768).' },
        { name: 'infoFrom', type: 'AppShellBreakpoint', optional: true, description: '`split`: the width the third pane fits from. Default `xl` (1280).' },
        { name: 'resizable', type: 'boolean', optional: true, description: '`split`: whether the divider drags. Default `true`.' },
        { name: 'resizeLabel', type: 'string', optional: true, description: '`split`: names the divider. Default `"Resize panes"`.' },
        { name: 'paneScroll', type: 'boolean', optional: true, description: "`split`: the shell gives each pane its own `ScrollView` (default). Turn it off when the pane's own content scrolls — a `FlatList`, a virtualizer — rather than nesting two scrollers." },
        { name: 'topBar', type: 'ReactNode', optional: true, description: "The phone header: a sibling ABOVE the shell's columns, spanning it edge to edge, pinned to the top (sticky with document scroll). Nothing a consumer writes needs `position: fixed` or a negative margin to be a top bar." },
        { name: 'topBarVisibility', type: 'AppShellBarVisibility', options: ['compact', 'always'], optional: true, description: '`compact` (default): only while the nav is a drawer.' },
        { name: 'bottomBar', type: 'ReactNode', optional: true, description: 'The bottom bar: pinned to the bottom of the viewport (or of the frame when the shell does not own the page), with the bottom safe-area inset applied as its own padding. The content column reserves its MEASURED height, so the last item is never hidden behind it.' },
        { name: 'reserveBottomBarSpace', type: 'boolean', optional: true, description: 'Reserve the measured bar height in document/container content (default true). False keeps the bar over fullscreen content whose own scroller accounts for the occupied bottom edge. The bar still publishes its real edge occupancy. Fixed mode always leaves clearance to its child navigator/list.' },
        { name: 'bottomBarVisibility', type: 'AppShellBarVisibility', options: ['compact', 'always'], optional: true, description: '`compact` (default): only while the nav is a drawer.' },
        { name: 'floatingAction', type: 'ReactNode', optional: true, description: 'The compose/FAB corner — above the bottom bar when there is one.' },
        { name: 'floatingActionPlacement', type: "'start' | 'end'", options: ['start', 'end'], optional: true, description: 'Which corner. Default `end` (the right in a left-to-right layout).' },
        { name: 'active', type: 'boolean', optional: true },
        { name: 'navigation', type: 'AppShellNavigationItem[]', optional: true },
        { name: 'value', type: 'string', optional: true },
        { name: 'onValueChange', type: '(value: string) => void', optional: true },
        { name: 'navigationPlacement', type: 'AppShellNavigationPlacement', options: ['auto', 'bottom', 'rail', 'sidebar'], optional: true },
        { name: 'navigationMaterial', type: "'solid' | 'translucent'", options: ['solid', 'translucent'], optional: true },
        { name: 'bottomActionBehavior', type: "'hide' | 'visible'", options: ['hide', 'visible'], optional: true, description: 'Bottom action follows scroll collapse by hiding (default), or remains visible.' },
        { name: 'scroll', type: "AppShellScroll | 'auto' | 'external'", options: ['document', 'container', 'fixed', 'auto', 'external'], optional: true, description: 'Web defaults to document; native to auto. Explicit auto owns a ScrollView. external: the child list owns scrolling; avoids nested virtualized lists.' },
        { name: 'primaryAction', type: "import('../fab/types').FabProps", optional: true },
      ],
    },
    'AppShellHeaderProps': {
      props: [
        { name: 'title', type: 'string', optional: true, description: 'Optional: a header can be just the menu button, breadcrumb or actions.' },
        { name: 'breadcrumb', type: 'ReactNode', optional: true },
        { name: 'actions', type: 'ReactNode', optional: true },
        { name: 'onMenuPress', type: '() => void', optional: true, description: 'Shows the hamburger (below `lg`); omit for no menu button.' },
        { name: 'menuOpen', type: 'boolean', optional: true, description: 'Swaps the hamburger for a close glyph while the drawer is open.' },
        { name: 'showMenu', type: 'boolean', optional: true, description: "Whether the hamburger has anything to open. `AppShell` passes its own answer, taken from the shell's MEASURED width and `navFrom`; standalone the header falls back to the window being narrower than `lg`." },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true },
        { name: 'testID', type: 'string', optional: true },
      ],
    },
    'AppShellMenuButtonProps': {
      props: [
        { name: 'accessibilityLabel', type: 'string', optional: true, description: 'Defaults to `"Open navigation"`.' },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true },
        { name: 'testID', type: 'string', optional: true },
      ],
    },
    'NotificationBellProps': {
      props: [
        { name: 'notifications', type: 'NotificationCenterItem[]', optional: true },
        { name: 'unreadCount', type: 'number', optional: true, description: 'Overrides the count on the glyph; defaults to the unread items.' },
        { name: 'onAction', type: '(notificationId: string, actionId: string) => void', optional: true },
        { name: 'onMarkAllRead', type: '() => void', optional: true },
        { name: 'defaultTab', type: 'NotificationCenterTab', options: ['all', 'mentions', 'system'], optional: true },
        { name: 'width', type: 'number', optional: true, description: 'Popover width. Default 440.' },
        { name: 'accessibilityLabel', type: 'string', optional: true },
        { name: 'testID', type: 'string', optional: true },
      ],
    },
    'ProOfferCardProps': {
      props: [
        { name: 'title', type: 'ReactNode', optional: false },
        { name: 'description', type: 'ReactNode', optional: false },
        { name: 'ctaLabel', type: 'string', optional: false, description: "The call to action's label." },
        { name: 'onCtaPress', type: '() => void', optional: true },
        { name: 'onDismiss', type: '() => void', optional: false, description: "Called by the close control; persisting the dismissal is the host's call." },
        { name: 'logo', type: 'ReactNode', optional: true, description: 'The mark above the copy (an animated Pro logo).' },
        { name: 'backdrop', type: 'ReactNode', optional: true, description: 'Light across the top, masked out before it reaches the copy.' },
        { name: 'backdropHeight', type: 'number', optional: true },
        { name: 'accessibilityLabel', type: 'string', optional: true },
        { name: 'dismissLabel', type: 'string', optional: true },
        { name: 'placement', type: "'fixed' | 'inline'", options: ['fixed', 'inline'], optional: true, description: '`fixed` (default) anchors bottom-left of the viewport (web) / the nearest positioned parent (native); `inline` renders in flow.' },
        { name: 'enterDelay', type: 'number', optional: true, description: 'Delay before the card rises in, ms. Defaults to 1200.' },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true },
        { name: 'testID', type: 'string', optional: true },
      ],
    },
  },
  components: [
    { name: 'AppShell', propsType: 'AppShellProps' },
    { name: 'AppShellHeader', propsType: 'AppShellHeaderProps' },
    { name: 'AppShellMenuButton', propsType: 'AppShellMenuButtonProps' },
    { name: 'NotificationBell', propsType: 'NotificationBellProps' },
    { name: 'ProOfferCard', propsType: 'ProOfferCardProps' },
  ],
}

// Generated by scripts/generate-bloom-catalog.ts. Do not edit.

import type { BloomSurfaceProps } from '../bloom-catalog'

export const props: BloomSurfaceProps = {
  subpath: 'chip',
  propTypes: {
    'ChipProps': {
      props: [
        { name: 'appearance', type: 'BloomAppearance', options: ['solid', 'subtle', 'outline', 'plain'], optional: true },
        { name: 'tone', type: 'BloomTone', options: ['neutral', 'accent', 'support', 'action', 'success', 'warning', 'danger', 'info'], optional: true },
        { name: 'checked', type: 'boolean', optional: true },
        { name: 'onCheckedChange', type: '(checked: boolean) => void', optional: true },
        { name: 'leading', type: 'React.ReactNode', optional: true },
        { name: 'trailing', type: 'React.ReactNode', optional: true },
        { name: 'leadingIcon', type: 'import("../icons/icon-component").BloomIconComponent', optional: true },
        { name: 'trailingIcon', type: 'import("../icons/icon-component").BloomIconComponent', optional: true },
        { name: 'children', type: 'React.ReactNode', optional: true, description: 'Text content of the chip.' },
        { name: 'variant', type: 'ChipVariant', options: ['solid', 'subtle', 'outlined', 'inverted'], optional: true, description: 'How loudly the chip is painted. `subtle` used to be spelled `soft` here and `subtle` on `Badge` — one concept, two names, resolved by the shared AccentFill.' },
        { name: 'color', type: 'AccentTone', options: ['default', 'primary', 'secondary', 'tertiary', 'success', 'warning', 'error', 'info'], optional: true, description: 'Semantic color.' },
        { name: 'hue', type: 'ChipHue', options: ['lime', 'rose', 'yellow', 'cyan', 'blue', 'purple', 'neutral', 'gray', 'soft'], optional: true, description: 'Paints the chip in one of the data hues instead of `color` + `variant` (both are ignored while it is set; `selected` still promotes the chip to the brand tone). Pair it with a size for the right emphasis: `bold` is `medium`, `subtle` is `large`, `caption` is `small`.' },
        { name: 'surface', type: 'string', optional: true, description: "The colour the chip sits on. A hue's dark fill is translucent, so it is mixed over this. Defaults to the page background." },
        { name: 'size', type: 'ChipSize', options: ['medium', 'sm', 'md', 'lg', 'xl', 'xs', 'small', 'large', '2xl'], optional: true, description: 'Size preset.' },
        { name: 'startIcon', type: 'React.ReactNode', optional: true, description: 'Icon rendered before the label.' },
        { name: 'endIcon', type: 'React.ReactNode', optional: true, description: 'Icon or close button rendered after the label.' },
        { name: 'onPress', type: '() => void', optional: true, description: 'Called when the chip is pressed. Makes the chip interactive.' },
        { name: 'onClose', type: '() => void', optional: true, description: 'Called when the close/end icon is pressed.' },
        { name: 'closeLabel', type: 'string', optional: true, description: 'Names the close button. Defaults to `"Remove <label>"` when the chip\'s label is a string, and to `"Remove"` when it is not — a row of removable chips whose buttons are all called "Remove" gives a screen reader no way to say which one it is about.' },
        { name: 'selected', type: 'boolean', optional: true, description: 'Whether the chip is in a selected state.' },
        { name: 'role', type: 'ChipRole', options: ['button', 'radio', 'tab'], optional: true, description: 'What a pressable chip is to assistive technology. Default `button` (`aria-pressed`). Use `radio` inside a `radiogroup` and `tab` inside a `tablist` — the state attribute follows the role, and a `radio` announcing `aria-pressed` is invalid.' },
        { name: 'disabled', type: 'boolean', optional: true, description: 'Whether the chip is disabled.' },
        { name: 'tabIndex', type: 'number', optional: true, description: 'Web only, for a roving-tabindex row: `-1` takes the chip out of the tab order so the row is a single tab stop. Inert on native.' },
        { name: 'onKeyDown', type: '(event: { key: string; preventDefault: () => void; }) => void', optional: true, description: 'Web only: key handling for a roving-tabindex row (arrows move focus). Inert on native.' },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true },
        { name: 'textStyle', type: 'StyleProp<TextStyle>', optional: true },
        { name: 'accessibilityLabel', type: 'string', optional: true },
        { name: 'testID', type: 'string', optional: true },
      ],
      inheritsFrom: ['RefAttributes<View>'],
    },
    'ChipRowProps': {
      props: [
        { name: 'children', type: 'React.ReactNode', optional: true, description: 'The pills.' },
        { name: 'gap', type: 'number', optional: true, description: 'Between pills. Default 8.' },
        { name: 'contentInset', type: 'number', optional: true, description: 'Side padding inside the scroller, so the first and last pill clear the edge.' },
        { name: 'fadeColor', type: 'string', optional: true, description: 'The colour the web edge fades blend into — the surface behind the row. Default the page background.' },
        { name: 'ringInset', type: 'number', optional: true, description: "Room kept on every side for a pill's focus ring, taken back out again with negative margins so the row still lines up with what is above it. Default 4." },
        { name: 'role', type: "'group' | 'radiogroup' | 'tablist'", options: ['group', 'radiogroup', 'tablist'], optional: true, description: 'What the row is: a `group` of toggles, a `radiogroup`, or a `tablist`. Default `group`.' },
        { name: 'accessibilityLabel', type: 'string', optional: true, description: 'Names the row. Required for anything but a plain `group` of self-naming pills.' },
        { name: 'disabled', type: 'boolean', optional: true, description: 'Marks the whole row disabled to assistive technology (web `aria-disabled`).' },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true },
        { name: 'contentContainerStyle', type: 'StyleProp<ViewStyle>', optional: true },
        { name: 'testID', type: 'string', optional: true },
      ],
    },
  },
  components: [
    { name: 'Chip', propsType: 'ChipProps' },
    { name: 'ChipRow', propsType: 'ChipRowProps' },
  ],
}

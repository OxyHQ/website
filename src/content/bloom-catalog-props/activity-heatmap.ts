// Generated by scripts/generate-bloom-catalog.ts. Do not edit.

import type { BloomSurfaceProps } from '../bloom-catalog'

export const props: BloomSurfaceProps = {
  subpath: 'activity-heatmap',
  propTypes: {
    'ActivityHeatmapProps': {
      props: [
        { name: 'data', type: 'ActivityHeatmapDay[]', optional: false, description: 'Per-day activity counts. Days not present are treated as count 0.' },
        { name: 'numDays', type: 'number', optional: true, description: 'Number of days to render, ending at `endDate`.' },
        { name: 'endDate', type: 'string', optional: true, description: 'Last (rightmost) day of the grid as `YYYY-MM-DD`. Defaults to the latest date present in `data`. The component never reads the system clock, so pass `endDate` explicitly to anchor the grid to "today".' },
        { name: 'levels', type: 'number[]', optional: true, description: 'Ascending count thresholds partitioning positive counts into color levels. With N thresholds a positive count maps to `colorScale[number of thresholds met]` (clamped to the scale). Count 0 always uses `emptyColor`.' },
        { name: 'colorScale', type: 'string[]', optional: true, description: 'Colors low→high. Index 0 is the faintest activity shade, the last index the strongest. Defaults to four opacity steps of `theme.colors.primary`.' },
        { name: 'emptyColor', type: 'string', optional: true, description: 'Color for zero-activity days.' },
        { name: 'cellSize', type: 'number', optional: true, description: 'Side length of each day cell in px.' },
        { name: 'gap', type: 'number', optional: true, description: 'Gap between cells in px (both axes).' },
        { name: 'weekStartsOn', type: '0 | 1', optional: true, description: 'First day of the week: 0 = Sunday, 1 = Monday. Controls both the row order and how the leading partial week aligns.' },
        { name: 'monthLabels', type: 'string[]', optional: true, description: '12 short month names indexed by month (0 = January), for the month-label row. Pass localized names; defaults to English abbreviations.' },
        { name: 'weekdayLabels', type: 'string[]', optional: true, description: '7 short weekday names indexed by day of week (0 = Sunday). When provided, a weekday-label column is rendered to the left of the grid. Omit for no weekday column. Pass empty strings for rows you want left blank.' },
        { name: 'onPressDay', type: '(day: ActivityHeatmapDay) => void', optional: true, description: 'Called with the pressed day. When set, each populated cell is pressable.' },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true, description: 'Container style override.' },
        { name: 'testID', type: 'string', optional: true },
      ],
    },
  },
  components: [
    { name: 'ActivityHeatmap', propsType: 'ActivityHeatmapProps' },
  ],
}

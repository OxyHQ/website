// Generated by scripts/generate-bloom-catalog.ts. Do not edit.

import type { BloomSurfaceProps } from '../bloom-catalog'

export const props: BloomSurfaceProps = {
  subpath: 'task-list',
  propTypes: {
    'TaskListProps': {
      props: [
        { name: 'tasks', type: 'ReadonlyArray<TaskListTask>', optional: false },
        { name: 'run', type: 'boolean', optional: true, description: 'Pauses and resumes the reveal. Hold it false to keep the log from starting on mount, then flip it true when the user sends. Toggling it back resumes where it stopped; to replay from the top, change the element `key`. Default `true`.' },
        { name: 'stepInterval', type: 'number', optional: true, description: 'Milliseconds between reveals. Default 850.' },
        { name: 'startDelay', type: 'number', optional: true, description: 'Milliseconds before the first reveal. Default 320.' },
        { name: 'revealed', type: 'number', optional: true, description: 'Drive the reveal yourself from real agent events. Counts units, where each task contributes one header plus one per step. Disables the internal timer.' },
        { name: 'collapseOnComplete', type: "boolean | 'all'", optional: true, description: 'Collapse finished tasks down to their headers. `true` collapses each task the moment its own steps have landed, so the log tidies itself as it runs. `"all"` instead holds every task open until the whole run lands, then collapses them together as one motion. Default `false`.' },
        { name: 'working', type: 'string | false', optional: true, description: "The indicator at the tail of the log while it is still running, so it always ends on the thing being worked on. Pass a label to change it, or `false` to drop it. Default `'Working'`." },
        { name: 'onComplete', type: '() => void', optional: true, description: 'Fires once, after the last step lands.' },
        { name: 'reduce', type: 'boolean', optional: true, description: 'Skip every reveal and swap. Defaults to the system reduced-motion setting.' },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true },
        { name: 'testID', type: 'string', optional: true },
      ],
    },
  },
  components: [
    { name: 'TaskList', propsType: 'TaskListProps' },
  ],
}

// Generated by scripts/generate-bloom-catalog.ts. Do not edit.

import type { BloomSurfaceProps } from '../bloom-catalog'

export const props: BloomSurfaceProps = {
  subpath: 'avatar-group',
  propTypes: {
    'AvatarGroupProps': {
      props: [
        { name: 'items', type: 'AvatarGroupItem[]', optional: false, description: 'Members to render, in stacking order (first item sits on top).' },
        { name: 'layout', type: "'stack' | 'row' | 'cluster'", options: ['stack', 'row', 'cluster'], optional: true, description: 'How the avatars are arranged. - `\'stack\'` (default): overlapping horizontal facepile with a thin separator ring, a trailing `+N` overflow chip, and the first item on top. - `\'row\'`: avatars placed adjacent with a positive `spacing` gap and NO separator ring — an "adjacent row" of icons (e.g. a top-tokens strip). Still collapses into the `+N` overflow chip at `max`. - `\'cluster\'`: a compact 2D "magnetic bubble cluster" (iMessage-style) — avatars of VARYING sizes deterministically packed into a round bounding box with a uniform gap: the first item (primary) is the largest and sits centred/in front, the rest nestle around it. Scales from 2 (a "front + behind" pair) up through a dense pack (~20), collapsing into a trailing `+N` bubble past `max`. Unlike the horizontal layouts, `size` is the overall box diameter (see `size`) and `max` defaults to 20 (see `max`), so it drops in where a single round Avatar would.' },
        { name: 'size', type: 'number', optional: true, description: "Diameter in pixels. For `'stack'`/`'row'` this is the diameter of EACH avatar. For `'cluster'` this is the diameter of the whole bounding box (the packed bubbles are sized as fractions of it), so the cluster occupies the same footprint as a single `size`-px Avatar." },
        { name: 'variant', type: 'string', optional: true, description: "Rendition variant forwarded to each Avatar 's `variant` prop, which the consumer's ImageResolver uses to build the right rendition for file-ID items. Stacked avatars are small, so `'thumb'` is recommended. Defaults to `'thumb'`; pass `undefined` to request full-size renditions." },
        { name: 'max', type: 'number', optional: true, description: "Maximum number of avatars to render before collapsing into the overflow chip. Defaults to 5 for `'stack'`/`'row'`, and to 20 for `'cluster'` (which packs densely). In `'cluster'` the cap is inclusive of the `+N` bubble: past the cap the last slot becomes the overflow bubble." },
        { name: 'total', type: 'number', optional: true, description: 'Real total count of members. When provided and larger than the number of rendered avatars, the overflow chip shows `+ (total - shown)`. When omitted, overflow is derived from `items.length - shown`.' },
        { name: 'overlap', type: 'number', optional: true, description: "Horizontal overlap between adjacent avatars in pixels. Defaults to ~1/3 of `size`. Larger values pull avatars closer together. `layout='stack'` only." },
        { name: 'spacing', type: 'number', optional: true, description: "Horizontal gap between adjacent avatars in pixels when `layout='row'`. Defaults to ~1/3 of `size`. `layout='row'` only." },
        { name: 'ringColor', type: 'string', optional: true, description: 'Color of the thin ring/border drawn around each avatar so overlapping avatars separate cleanly. Defaults to the theme `background` color so the avatars read as punched out of the surface behind them.' },
        { name: 'showInitials', type: 'boolean', optional: true, description: "When true, an item with no `uri` renders Avatar 's colored deterministic initial (derived from its name) instead of the neutral default placeholder. Use for decorative/icon rows (e.g. a top-tokens strip) where distinct colors read better than identical grey circles. Defaults to `false` to preserve the neutral facepile placeholder for avatar-less people." },
        { name: 'onPressItem', type: '(item: AvatarGroupItem, index: number) => void', optional: true, description: 'Per-avatar press handler. When provided, each avatar (and the overflow chip) becomes pressable. When omitted, the group is purely presentational.' },
        { name: 'hoverCard', type: 'boolean', optional: true, description: 'Enables the hover card on web. Hovering an avatar reveals a UserHoverCard for that item. No-op on native.' },
        { name: 'renderItemAction', type: '(item: AvatarGroupItem, index: number) => ReactNode', optional: true, description: "Per-item action slot rendered inside the hover card — typically the app's SDK FollowButton. Only used when `hoverCard` is enabled (web)." },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true, description: 'Container style override.' },
        { name: 'testID', type: 'string', optional: true },
      ],
    },
  },
  components: [
    { name: 'AvatarGroup', propsType: 'AvatarGroupProps' },
  ],
}

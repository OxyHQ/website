// Generated by scripts/generate-bloom-catalog.ts. Do not edit.

import type { BloomSurfaceProps } from '../bloom-catalog'

export const props: BloomSurfaceProps = {
  subpath: 'fab',
  propTypes: {
    'FabProps': {
      props: [
        { name: 'label', type: 'string', optional: true },
        { name: 'iconSize', type: 'number', optional: true, description: 'Glyph dimensions in points; defaults to the selected size’s icon scale.' },
        { name: 'collapsed', type: 'boolean', optional: true, description: 'Animate the extended label away while retaining the action and accessible name.' },
        { name: 'children', type: 'ReactNode', optional: true },
        { name: 'size', type: 'BloomSize | FabSize | number', optional: true },
        { name: 'variant', type: 'FabVariant', options: ['primary', 'secondary', 'tertiary', 'surface'], optional: true, description: 'Existing color preset. Explicit tone takes precedence.' },
        { name: 'placement', type: 'FabPlacement', options: ['bottom-right', 'bottom-left', 'top-right', 'top-left', 'static'], optional: true },
        { name: 'offset', type: 'number', optional: true },
        { name: 'minimizeBehavior', type: 'FabMinimizeBehavior', options: ['none', 'hide', 'collapse'], optional: true },
        { name: 'labelStyle', type: 'StyleProp<TextStyle>', optional: true },
        { name: 'zIndex', type: 'number', optional: true },
        { name: 'accessibilityHint', type: 'string', optional: true },
        { name: 'pressed', type: 'boolean', optional: true, description: 'Toggle state, announced on web and native. Omit for ordinary actions.' },
        { name: 'stopPropagation', type: 'boolean', optional: true, description: 'Keep an embedded action from also activating its parent. Default false.' },
        { name: 'appearance', type: 'BloomAppearance', options: ['solid', 'subtle', 'outline', 'plain'], optional: true },
        { name: 'tone', type: 'BloomTone', options: ['neutral', 'accent', 'support', 'action', 'success', 'warning', 'danger', 'info'], optional: true },
        { name: 'onPress', type: '() => void', optional: true },
        { name: 'disabled', type: 'boolean', optional: true },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true },
        { name: 'textStyle', type: 'StyleProp<TextStyle>', optional: true },
        { name: 'icon', type: 'React.ReactNode | ButtonIconComponent', optional: true, description: 'An icon ELEMENT rendered as-is, or an icon COMPONENT (`icon={RiMore2Line}`), which the button sizes and colours like `leadingIcon`.' },
        { name: 'iconPosition', type: "'left' | 'right'", options: ['left', 'right'], optional: true },
        { name: 'linkTone', type: 'ButtonLinkTone', options: ['primary', 'secondary', 'text'], optional: true, description: "The `link` variant's colour. `primary` is the accent label, `secondary` the secondary-text label, `text` the READING colour (`text` at rest, secondary under a pointer or a press) — the tone an underlined action takes when it sits inside a sentence and must not read as the accent. Ignored by other variants." },
        { name: 'underline', type: 'ButtonUnderline', options: ['rest', 'hover', 'none'], optional: true, description: "When the label carries its underline. `rest` underlines always — the underline IS the affordance, which is what an inline text action inside running copy needs; `hover` underlines only under a pointer (the `link` variant's default, and web-only: native has no hover); `none` never does." },
        { name: 'textVariant', type: 'TypeScaleVariant', options: ['large-title-regular', 'large-title-medium', 'large-title-semibold', 'large-title-bold', 'display-1-regular', 'display-1-medium', 'display-1-semibold', 'display-1-bold', 'display-2-regular', 'display-2-medium', 'display-2-semibold', 'display-2-bold', 'display-3-regular', 'display-3-medium', 'display-3-semibold', 'display-3-bold', 'display-4-regular', 'display-4-medium', 'display-4-semibold', 'display-4-bold', 'title-1-regular', 'title-1-medium', 'title-1-semibold', 'title-1-bold', 'title-2-regular', 'title-2-medium', 'title-2-semibold', 'title-2-bold', 'title-3-regular', 'title-3-medium', 'title-3-semibold', 'title-3-bold', 'headline-regular', 'headline-medium', 'headline-semibold', 'headline-bold', 'body-regular', 'body-medium', 'body-semibold', 'body-bold', 'body-2-regular', 'body-2-medium', 'body-2-semibold', 'body-2-bold', 'caption-1-regular', 'caption-1-medium', 'caption-1-semibold', 'caption-1-bold', 'caption-2-regular', 'caption-2-medium', 'caption-2-semibold', 'caption-2-bold'], optional: true, description: "Override the type-ramp step the size picks for the label (`body-semibold`, `body-2-medium`, …). The four families that hand-rolled an inline text button each drew their label at their own step, and the ONE thing they could not express through `textStyle` is the line height: on web a `lineHeight` number in a style object is CSS's UNITLESS multiplier, so a ramp step passed that way renders at twenty times its size. This is read from `TYPE_SCALE` and emitted in px by the web fork, and handed to `Text` as its `variant` by the native one." },
        { name: 'numberOfLines', type: 'number', optional: true, description: 'Truncate the label instead of letting it wrap. Native honours any count (`Text numberOfLines`); the web fork honours `1`, as an ellipsis on the label — `.bloom-btn` is already `white-space: nowrap; overflow: hidden`, so a longer label was CLIPPED there rather than wrapped, with nothing to say it had been.' },
        { name: 'accessibilityRole', type: "'button' | 'link'", options: ['button', 'link'], optional: true, description: 'Override the announced role. `link` WITHOUT an `href` is the one reason this exists: a family whose control navigates through its own router (or jumps within the page) renders no anchor, and both `listing-details` and `media-header` had already decided their inline links announce as links on web and as buttons on native. Prefer `href` — it renders a REAL anchor, which is the only spelling middle-click, copy-link and the status bar understand.' },
        { name: 'href', type: 'string', optional: true, description: 'Makes the button a link. On web it renders a real `<a href>` (dropped while disabled); on native a press opens the URL unless `onPress` is set.' },
        { name: 'target', type: 'string', optional: true, description: 'Anchor `target` (web only, with `href`).' },
        { name: 'rel', type: 'string', optional: true, description: 'Anchor `rel` (web only, with `href`).' },
        { name: 'renderLeadingIcon', type: 'BloomIconRenderer', optional: true, description: "Draw the leading glyph yourself, handed the button's icon size and the foreground for the CURRENT state — for an app's own icon set, whose props Bloom's icon slot cannot name. Wins over `leadingIcon`, and is used for the glyph when `iconOnly`. See `icons/render-icon.tsx`." },
        { name: 'renderTrailingIcon', type: 'BloomIconRenderer', optional: true, description: 'Same as ButtonProps.renderLeadingIcon, after the label. Ignored when `iconOnly`.' },
        { name: 'iconOnly', type: 'boolean', optional: true, description: 'Render a square icon-only button (24 / 32 / 36 / 44) from `leadingIcon` or `icon`. Name it with `accessibilityLabel` — there is no text to read.' },
        { name: 'loading', type: 'boolean', optional: true, description: 'When true, displays a centered loading spinner overlay and prevents presses. Children remain in the layout (but visually hidden) so the button preserves its width. Use this for async actions like submit.' },
        { name: 'loadingColor', type: 'string', optional: true, description: 'Optional color override for the loading spinner. Defaults to the resolved button text color.' },
        { name: 'accessibilityLabel', type: 'string', optional: true },
        { name: 'hitSlop', type: '{ top: number; bottom: number; left: number; right: number; }', optional: true },
        { name: 'testID', type: 'string', optional: true },
        { name: 'className', type: 'string', optional: true },
        { name: 'type', type: "'button' | 'submit' | 'reset'", options: ['button', 'submit', 'reset'], optional: true, description: "HTML button `type` (web only). Defaults to `'button'`. Use `'submit'` to participate in `<form>` submission / HTML5 validation / Enter-to-submit, and `'reset'` to reset a form." },
        { name: 'onClick', type: '(event: React.MouseEvent<HTMLButtonElement>) => void', optional: true, description: 'Web click handler. Aliased to the same logical action as `onPress` — either (or both) may be provided; both fire on click. `onPress` is kept for cross-platform parity.' },
        { name: 'asChild', type: 'boolean', optional: true, description: 'Render as the child element instead of a `<button>` (web only). Use for link-buttons: `<Button asChild><a href="…">Go</a></Button>` applies the button styling to the `<a>` (or a router `<Link>`). The child receives the merged `className`, `style`, and the button\'s event handlers.' },
        { name: 'id', type: 'string', optional: true, description: 'Inline DOM `id` (web only).' },
        { name: 'name', type: 'string', optional: true, description: 'Native HTML `name` for form participation (web only).' },
        { name: 'value', type: 'string', optional: true, description: 'Native HTML `value` for form participation (web only).' },
        { name: 'aria-label', type: 'string', optional: true, description: '`aria-label` (web only). Falls back to `accessibilityLabel`.' },
        { name: 'aria-expanded', type: 'boolean', optional: true, description: 'Whether the surface this button controls is open.' },
        { name: 'aria-haspopup', type: "WebAriaProps['aria-haspopup']", options: ['menu', 'listbox', 'tree', 'grid', 'dialog'], optional: true, description: "What the surface this button opens IS. Forwarded for the same reason as ['aria-expanded']; the value comes from the family." },
        { name: 'title', type: 'string', optional: true, description: '`title` tooltip attribute (web only).' },
        { name: 'autoFocus', type: 'boolean', optional: true, description: '`autoFocus` the button on mount (web only).' },
        { name: 'tabIndex', type: 'number', optional: true, description: '`tabIndex` override (web only).' },
        { name: 'fullWidth', type: 'boolean', optional: true, description: 'Stretch the button to fill its container\'s main axis (web only). Equivalent to passing `className="w-full"`; provided as a prop for ergonomics.' },
      ],
    },
  },
  components: [
    { name: 'Fab', propsType: 'FabProps' },
  ],
}

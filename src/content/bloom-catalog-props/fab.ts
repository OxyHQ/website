// Generated by scripts/generate-bloom-catalog.ts. Do not edit.

import type { BloomSurfaceProps } from '../bloom-catalog'

export const props: BloomSurfaceProps = {
  subpath: 'fab',
  propTypes: {
    'FabProps': {
      props: [
        { name: 'onPress', type: '() => void', optional: true, description: 'Press handler.' },
        { name: 'icon', type: 'React.ReactNode', optional: true, description: 'The icon to render inside the FAB. Typically a Bloom icon element (`<Icons.PlusLarge_Stroke2_Corner0_Rounded />` — icon exports carry their full style and corner suffix, and there is no shortened alias). For an icon-only FAB this is the whole content; for the extended variant it sits to the left of the `label`.' },
        { name: 'children', type: 'React.ReactNode', optional: true },
        { name: 'label', type: 'string', optional: true, description: 'When provided, renders the EXTENDED FAB: a pill with the icon followed by this text label. Omit for the canonical circular icon FAB.' },
        { name: 'variant', type: 'FabVariant', options: ['primary', 'secondary', 'tertiary', 'surface'], optional: true },
        { name: 'size', type: 'FabSize | number', optional: true, description: "A preset (`'small' | 'medium' | 'large'`) or a raw pixel diameter (`number`). Defaults to `'medium'`. See FabSize." },
        { name: 'placement', type: 'FabPlacement', options: ['bottom-right', 'bottom-left', 'top-right', 'top-left', 'static'], optional: true, description: "Corner of the containing block to anchor to. Defaults to `'bottom-right'`. See FabPlacement. The DEFAULT never escapes the containing column (web uses `position: sticky`, native uses `position: absolute`)." },
        { name: 'offset', type: 'number', optional: true, description: 'Distance (in px) from the anchored edges. Applies to whichever edges the `placement` pins to. Defaults to `16`.' },
        { name: 'disabled', type: 'boolean', optional: true },
        { name: 'accessibilityLabel', type: 'string', optional: true, description: 'Required for icon-only FABs so screen readers announce the action.' },
        { name: 'accessibilityHint', type: 'string', optional: true },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true, description: 'Container style override (merged last).' },
        { name: 'labelStyle', type: 'StyleProp<TextStyle>', optional: true, description: 'Extended-FAB label text style override.' },
        { name: 'className', type: 'string', optional: true, description: 'NativeWind / DOM className passthrough.' },
        { name: 'testID', type: 'string', optional: true },
        { name: 'zIndex', type: 'number', optional: true, description: 'Hard `zIndex` override. Defaults to a high value so the FAB floats above scrolling content.' },
        { name: 'onClick', type: '(event: React.MouseEvent<HTMLButtonElement>) => void', optional: true, description: 'Web click handler. Fires together with `onPress`.' },
        { name: 'aria-label', type: 'string', optional: true, description: '`aria-label` (web only). Falls back to `accessibilityLabel`.' },
        { name: 'title', type: 'string', optional: true, description: '`title` tooltip attribute (web only).' },
        { name: 'id', type: 'string', optional: true, description: 'Inline DOM `id` (web only).' },
        { name: 'type', type: "'button' | 'submit' | 'reset'", options: ['button', 'submit', 'reset'], optional: true, description: "HTML button `type` (web only). Defaults to `'button'`." },
      ],
    },
  },
  components: [
    { name: 'Fab', propsType: 'FabProps' },
  ],
}

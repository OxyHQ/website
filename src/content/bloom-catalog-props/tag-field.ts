// Generated by scripts/generate-bloom-catalog.ts. Do not edit.

import type { BloomSurfaceProps } from '../bloom-catalog'

export const props: BloomSurfaceProps = {
  subpath: 'tag-field',
  propTypes: {
    'TagFieldProps': {
      props: [
        { name: 'value', type: 'ReadonlyArray<string>', optional: false, description: 'The committed tags, in order. Controlled — the app owns the list.' },
        { name: 'onChange', type: '(value: ReadonlyArray<string>) => void', optional: false, description: 'Called with the next list, on an add and on a remove.' },
        { name: 'inputValue', type: 'string', optional: true, description: 'The text being typed, when the app wants to own it (to drive a remote suggestion query). Omitted, the field keeps it internally.' },
        { name: 'onInputValueChange', type: '(text: string) => void', optional: true, description: 'Called with every keystroke. Given with `inputValue`, this is the only way the text changes.' },
        { name: 'placeholder', type: 'string', optional: true, description: 'Drawn while the field is empty.' },
        { name: 'suggestions', type: 'ReadonlyArray<string | TagSuggestion>', optional: true, description: 'What to offer under the field. Filtered by what has been typed and by what is already committed, so an app can hand over its whole vocabulary.' },
        { name: 'maxSuggestions', type: 'number', optional: true, description: 'How many suggestions to draw. Default `6`.' },
        { name: 'allowCreate', type: 'boolean', optional: true, description: 'Whether a tag that is not in `suggestions` can be committed. Default `true`. `false` makes the field a picker over a closed vocabulary.' },
        { name: 'max', type: 'number', optional: true, description: 'The ceiling. At the ceiling the input stops accepting and the hint says so.' },
        { name: 'onMaxReached', type: '() => void', optional: true, description: 'Called when a commit was refused because the field is full.' },
        { name: 'normalize', type: '(raw: string) => string', optional: true, description: 'How a raw string becomes a tag before it is compared and stored — trimmed by default. Comparison is done on the NORMALISED form, so `" Design "` and `"Design"` are one tag and the second is dropped in silence.' },
        { name: 'tone', type: 'AccentTone', options: ['default', 'primary', 'secondary', 'tertiary', 'success', 'warning', 'error', 'info'], optional: true, description: 'The tone the committed chips are painted in. Default `default`.' },
        { name: 'size', type: 'TagFieldSize', options: ['medium', 'sm', 'md', 'lg', 'xs', 'small'], optional: true, description: 'Default `medium` (36 tall); `small` is 32. Omitted, it is INHERITED from the nearest `ControlSurface` — the same vocabulary `TextField` and `Textarea` take — so a filter row asks for `small` once instead of writing it on five controls.' },
        { name: 'disabled', type: 'boolean', optional: true },
        { name: 'invalid', type: 'boolean', optional: true, description: "Paints the error shell. Combined with an enclosing `Field`'s, never replaced." },
        { name: 'label', type: 'string', optional: true, description: "The control's own name, for a field used outside a `Field`. Inside one the field's label wins — the words above the box and the announced name cannot disagree." },
        { name: 'accessibilityLabel', type: 'string', optional: true, description: "Always wins, over the control's own label AND over the field's." },
        { name: 'nativeID', type: 'string', optional: true, description: 'An id the caller wired itself. Outranks the one a `Field` supplies.' },
        { name: 'labels', type: 'TagFieldLabels', optional: true },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true },
        { name: 'testID', type: 'string', optional: true },
      ],
    },
  },
  components: [
    { name: 'TagField', propsType: 'TagFieldProps' },
  ],
}

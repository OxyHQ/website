// Generated by scripts/generate-bloom-catalog.ts. Do not edit.

import type { BloomSurfaceProps } from '../bloom-catalog'

export const props: BloomSurfaceProps = {
  subpath: 'level-picker',
  propTypes: {
    'LevelPickerProps': {
      props: [
        { name: 'levels', type: 'readonly string[]', optional: false, description: "The levels, in order, low to high. Its LENGTH is the number of stops and each entry is that stop's own name — read out as the slider's value (`aria-valuetext`), so it is what a screen reader says instead of an index." },
        { name: 'value', type: 'number', optional: false, description: 'The selected level, as an index into levels. Controlled.' },
        { name: 'onValueChange', type: '(value: number) => void', optional: false, description: 'Fired with the new index as the user drags, clicks or steps the rail.' },
        { name: 'accessibilityLabel', type: 'string', optional: false, description: "The slider's accessible NAME — WHAT the level applies to." },
        { name: 'minLabel', type: 'string', optional: true, description: 'Captions for the two ends of the rail, shown while the slider is active (hovered, focused or being dragged) and faded out otherwise, in the place the summary row occupies.' },
        { name: 'maxLabel', type: 'string', optional: true },
        { name: 'detailsLabel', type: 'string', optional: true, description: "The summary row's label — the disclosure that reveals children. Defaults to `Details`, matching the `<details>`/`<summary>` vocabulary this is the same shape as." },
        { name: 'expanded', type: 'boolean', optional: true, description: 'Whether the details region is showing. Controlled.' },
        { name: 'defaultExpanded', type: 'boolean', optional: true, description: 'Its initial state when uncontrolled. Defaults to `false`.' },
        { name: 'onExpandedChange', type: '(expanded: boolean) => void', optional: true },
        { name: 'children', type: 'ReactNode', optional: true, description: "The details region's contents — the rows the summary row reveals." },
        { name: 'className', type: 'string', optional: true, description: "Appended to the root's own classes, never substituted for them." },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true },
        { name: 'testID', type: 'string', optional: true },
      ],
    },
  },
  components: [
    { name: 'LevelPicker', propsType: 'LevelPickerProps' },
  ],
}

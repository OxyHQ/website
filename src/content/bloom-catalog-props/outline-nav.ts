// Generated by scripts/generate-bloom-catalog.ts. Do not edit.

import type { BloomSurfaceProps } from '../bloom-catalog'

export const props: BloomSurfaceProps = {
  subpath: 'outline-nav',
  propTypes: {
    'OutlineNavProps': {
      props: [
        { name: 'headings', type: 'ReadonlyArray<OutlineHeading>', optional: false, description: 'The headings, in document order.' },
        { name: 'activeId', type: 'string', optional: true, description: 'Which heading the reader is in. Marked `aria-current` and drawn with the rail.' },
        { name: 'onSelect', type: '(heading: OutlineHeading) => void', optional: true, description: 'Called when a heading is chosen. **The app does the jumping** — this component has no idea where the body is, and a scroll it performed itself would find the wrong scroller in a split view.' },
        { name: 'variant', type: 'OutlineNavVariant', options: ['full', 'compact'], optional: true, description: 'Default `full`.' },
        { name: 'progress', type: 'number', optional: true, description: "How far through the document the reader is, 0 to 1. Omitted, it is derived from `activeId`'s position — which is right for a jump list and wrong for a continuous scroll, so a screen that measures scroll passes its own." },
        { name: 'hideProgress', type: 'boolean', optional: true, description: 'Hides the progress bar. Default `false`.' },
        { name: 'title', type: 'string', optional: true, description: 'The label above the list in `full`. Default the `outline` label.' },
        { name: 'compactMaxLevel', type: 'number', optional: true, description: 'The deepest level `compact` draws. Default `2` — the levels below it are DROPPED rather than squeezed into an indent nobody can see on a phone. Ignored by `full`, which draws everything.' },
        { name: 'accessibilityLabel', type: 'string', optional: true, description: 'Names the navigation region. Defaults to the `outline` label.' },
        { name: 'labels', type: 'OutlineNavLabels', optional: true },
        { name: 'style', type: 'StyleProp<ViewStyle>', optional: true },
        { name: 'testID', type: 'string', optional: true },
      ],
    },
  },
  components: [
    { name: 'OutlineNav', propsType: 'OutlineNavProps' },
  ],
}

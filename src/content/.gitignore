# Synced docs content — generated by `bun scripts/sync-docs.ts`. Everything
# under `_synced/` is a build artifact and must not be committed (except for
# the placeholder `index.json` checked in below so the docs-loader still
# typechecks on a fresh clone before sync runs).
_synced/*
!_synced/index.json
